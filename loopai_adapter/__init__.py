"""Inner package for the loopai-adapter plugin.

The plugin directory is hyphenated (``loopai-adapter``) — matching the manager's
discovery of the other plugins — while this importable inner package is
underscored so its modules can use ordinary relative imports.
"""
